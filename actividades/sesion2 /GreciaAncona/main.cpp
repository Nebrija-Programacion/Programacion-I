#include <iostream>

using namespace std;

int main()
{
    int i , n , a = 0 ;
    cout << " Ingrese un numero : " << endl;
    cin >> n;
    for ( i{1} ; i < (n + 1 ) ; i ++ ){
        if (n%1 == 0 ) {
            a++ ;
        }
    };
    if(a! = 2) {
        cout << " no es un numero primo " << endl;
    } else {
        cout << " Si es primo " << endl;
    }
    return 0;
}
